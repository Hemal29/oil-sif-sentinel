# Backend — OIL SIF Sentinel

Node.js + Express.js API. See root README and `docs/` for architecture.
Business logic is intentionally not implemented yet — this is structure only.

Run:

```bash
cp .env.example .env
npm install
npm run dev
GET http://localhost:5000/api/v1/health
```

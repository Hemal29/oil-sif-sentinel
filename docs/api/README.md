# API (intended)

Base: /api/v1

- /auth, /reports, /analysis, /dashboard, /patterns, /reviews, /rules, /sites, /activities, /assistant
- GET /api/v1/health -> { success: true, message: ... }
- Full endpoint specs to be added in Step 2.
